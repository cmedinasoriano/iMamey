import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  background: {
    alignItems: 'center',
    overflow: 'hidden'
  },

  notificationIcon: {
    margin: 'auto',
  },

  icon: {
    position: 'absolute',
  },
})

export default styles;